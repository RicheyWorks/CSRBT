{
 "id": "adr147",
 "chain": "adr146b",
 "chain_probe": "tools/audit_targets.py",
 "subject": "ADR-147: one list, not two -- the files that were on disk and in no commit",
 "body": "Every slice of this kit has produced TWO hand-written lists of the same file set -- the tarball's, and the push script's `git add` -- and nothing compared them. When they disagreed the file reached the operator's disk and never reached a commit, and EVERY AUDIT IN THE KIT STAYED GREEN, because every audit measures the working tree and the working tree has the file. It happened: ADR-143 and ADR-144 shipped four files -- the three state audits printing the NAME of a never-exposed control in their summaries, and verify_organism printing the lines that differ when two consecutive physicals disagree -- and neither slice's push script named them. They sat modified and uncommitted for three days, through six full green runs, and were found only because a push script was run twice and the second run had nothing to stage. SO THERE IS ONE LIST NOW. A slice writes tools/delivery/<id>.json -- id, chain, subject, body, paths -- and tools/deliver.py generates BOTH artefacts from it: --script writes tools/push/push-<id>.ps1, --bundle writes the tarball containing exactly those paths plus the script, --record moves the delivery ledger forward. Neither list is hand-written again, and --check holds that: every path a manifest names exists, every generated script is BYTE-IDENTICAL to the one on disk (a hand edit is a failure rather than a silent divergence), a manifest's id matches its filename, and its chain names a manifest or a script that is really there. THE PUSH SCRIPT MOVES INTO THE REPO. It was the one artefact of every slice that lived outside it -- outside every audit, outside every suite, outside the commit it describes. ADR-096 to ADR-103 kept theirs in CSRBT/; from ADR-104 they were written to the parent directory instead, and the scripts for ADR-104 to ADR-111 no longer exist anywhere, so what those eight slices staged cannot now be read. Nothing noticed either the drift or the loss, because nothing was looking. tools/audit_delivery.py is the measurement. There is no git in the agent's copy (it is a mount, not a clone), so the evidence is CONTENT: the ledger holds the sha256 of the bytes each slice delivered, and anything on disk whose bytes are not in it has not been delivered. What makes it usable DURING a slice is the subtraction -- work in flight is declared by the slice's own manifest, the same list the script and the tarball come from. Name it in the manifest or the audit names it here. It fails by default with no flag, because run_all runs an audit with no arguments, and audit_delivery is registered there now. The ledger is SEEDED BY ADOPTION from a tree known to be committed, and the ledger says so: an adoption is not evidence that those bytes were pushed, it is the baseline the ratchet starts from. Held: this is not proof that a delivered file was PUSHED -- that happens on a machine this process cannot see, and the ledger records what was handed over, not what git did with it. It is the other half: a file nothing has ever handed over cannot have been pushed. Evidence directories, traces and binaries are outside the accounting, because a screenshot rewritten by every run is not an undelivered change. And this slice's own delivery is made by the new tool, which is the only way to find out whether it works.",
 "paths": [
  "tools/deliver.py",
  "tools/audit_delivery.py",
  "tools/delivery_ledger.json",
  "tools/delivery/adr147.json",
  "tools/verify/verify_delivery.py",
  "tools/mutate_delivery.py",
  "tools/verify/run_all.py",
  "tools/verify/counts.json",
  "tools/harness_board.py",
  "tools/harness_board.html",
  "tools/published.json",
  "tools/mutant_ledger.json",
  "tools/task_ledger.json",
  "tools/readable_ledger.json",
  "tools/entry_ledger.json",
  "docs/ADR-147-one-list-not-two-2026-09-06.md",
  "docs/CHANGELOG-2026-09-06-adr147-one-list-not-two.md",
  "docs/AI_HARNESS.md"
 ],
 "clean": [
  "adr128",
  "adr129",
  "adr130",
  "adr131",
  "adr132",
  "adr133",
  "adr134",
  "adr135",
  "adr136",
  "adr137",
  "adr138",
  "adr139",
  "adr140",
  "adr141",
  "adr142",
  "adr143",
  "adr144",
  "adr145",
  "adr146",
  "adr147"
 ]
}
