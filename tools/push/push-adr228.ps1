# push-adr228.ps1 -- generated by tools/deliver.py from tools/delivery/adr228.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr228.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr228.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr228 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR228 pushed."; exit 0 }
  Write-Host "adr228 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr227.ps1"), (Join-Path $root "push-adr227.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-227-the-page-that-graded-everything-and-was-graded-by-nothing-2026-09-17.md
  if ($st) { Write-Host "adr227 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-228-the-two-fields-an-operator-could-not-send-2026-09-18.md `
  docs/CHANGELOG-2026-09-18-adr228-the-two-fields-an-operator-could-not-send.md `
  tools/blind_console.py `
  tools/verify/verify_mcp.py `
  tools/verify/verify_tasks.py `
  tools/mutate_contract.py `
  tools/traces/blind6/PROVENANCE.md `
  tools/traces/blind6/page-greenhouse-science.jsonl.gz `
  tools/traces/blind6/page-soil-bench-science.jsonl.gz `
  tools/traces/blind6/page-survey-design-science.jsonl.gz `
  tools/traces/blind6/page-tree-visualizer-science.jsonl.gz `
  tools/traces/blind6/tasks/page-greenhouse-science.json `
  tools/traces/blind6/tasks/page-soil-bench-science.json `
  tools/traces/blind6/tasks/page-survey-design-science.json `
  tools/traces/blind6/tasks/page-tree-visualizer-science.json `
  tools/delivery/adr228.json `
  docs/AI_HARNESS.md `
  tools/harness_board.html `
  tools/mutant_ledger.json `
  tools/published.json `
  tools/verify/counts.json `
  tools/delivery_ledger.json `
  tools/push/push-adr228.ps1
git -C $csrbt commit -m "ADR-228: the two things a command says about itself, given to the operator at last -- and the first four science pages no blind operator had ever driven" `
  -m "ADR-222 gave the gateway if_stamp and expires_at and no operator could send either; blind_console now carries both in _meta, only when a move names them, so a plain call is unchanged. The sixth blind trial spent them on four pages no blind operator had touched -- greenhouse, soil bench, survey design, tree visualizer -- one attempt each, 55 of 78 outcomes and 239 of 285 claims in 171 calls. Every operator guarded its irreversible act with if_stamp and the door's refusal is in the trace: a stamp the target moved past refused stale on all four, a past expires_at refused expired, and after the guard the act ran. Found: every operator confused the report's inner stamp with the door's outer snapshot stamp (the next slice); and tree-visualizer makes 28 nodes where the task holds 24 because its random button draws until a key is new (filed). verify_mcp 93 to 98, verify_tasks 392 to 411, mutate_contract 103 to 105." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015Ryyc7gWVh4QXAQ9RAFPF1"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR228: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr228.json)) { Write-Error "ADR228: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR228: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr227.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR228 pushed."
