# push-adr160.ps1 -- generated by tools/deliver.py from tools/delivery/adr160.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr160.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr159.ps1"), (Join-Path $root "push-adr159.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- tools/tasks/page-cell-bench-science.json
  if ($st) { Write-Host "adr159 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  tools/tasks/page-farm-scout-science.json `
  tools/entry_ledger.json `
  tools/outputs_ledger.json `
  tools/task_ledger.json `
  tools/delivery_ledger.json `
  tools/verify/counts.json `
  tools/harness_board.html `
  tools/published.json `
  tools/delivery/adr160.json `
  docs/ADR-160-the-farm-scout-recorded-whole-2026-09-08.md `
  docs/CHANGELOG-2026-09-08-adr160-the-farm-scout-recorded-whole.md `
  docs/AI_HARNESS.md `
  tools/push/push-adr160.ps1
git -C $csrbt commit -m "ADR-160: the farm scout, recorded whole -- pest scouting, pollinators, germination and rotation, entered and exported" `
  -m "THE FARM SCOUT -- farm-scout.html, a grower's field day across four tools (pest scouting with a dispersion read, pollinator counts with Shannon diversity, germination with seeds-to-sow, and a rotation checker that is a PICKER PER SEASON PER BED because soil pests follow plant families not crop names) -- WAS THE KIT'S MOST-NEGLECTED DATA-ENTRY PAGE: 5 of 15 fields entered and BOTH its exports read by nothing. Its task tested germination, flagged a rotation conflict and counted a pest, but ten of its fifteen fields were never entered (the pest name, the field/observer/date stamp, the action threshold, every pollinator tally, the second season on bed one and all three on bed two) and neither the field-day report nor the CSV was ever read. The existing scenario is kept verbatim (ten-stop scout averaging 3.6 pests/plant with var/mean 2.54; germination 83% -> 65% -> 45%), so its assertions hold, and the rest of the field day is driven around it: the pest is named, the field stamped with block/observer/date, five pollinator types are tallied (four defaults plus ONE ADDED BY HAND, 11 visits, effective types 4.3, evenness 0.91), and the rotation is completed to both beds. Both exports are then read and held against an oracle that computes the Shannon diversity, the dispersion, the germination percentage and every export line the page's own way -- Shannon and evenness from the counts, Math.round on the germination, famLabel on the rotation keys -- so both sides of every claim come from the same arithmetic. farm-scout 5 -> 15 OF 15 FIELDS, 0 -> 2 exports held, 68 -> 117 confirmed and 0 refuted, and the kit 461 -> 471 of 521 (90%), 17 of 27 pages entered whole. Held: the rotation family selectors count as a field each (a picker per season, named by their first option), so completing bed two added a rotation line to both exports, folded into the oracle rather than worked around; the pollinator tallies are reached by host index (@control:poGrid#0) because a tally button's label carries its live count and changes as it is tapped. No page was edited -- a task-only slice, the tenth page driven end to end, and the kit's data-entry coverage crosses ninety percent." `
  -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_01CNn3hvazSDBU2TCgsGXjTt"
git -C $csrbt push
$t = Join-Path $root "_to_delete\adr128.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr129.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr130.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr131.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr132.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr133.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr134.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr135.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr136.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr137.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr138.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr139.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr140.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr141.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr142.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr143.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr144.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr145.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr146.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr147.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr148.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr149.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr150.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr151.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr152.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr153.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr154.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr155.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr156.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr157.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr158.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr159.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr160.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR160 pushed."
