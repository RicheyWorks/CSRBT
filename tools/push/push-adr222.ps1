# push-adr222.ps1 -- generated by tools/deliver.py from tools/delivery/adr222.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr222.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr222.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr222 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR222 pushed."; exit 0 }
  Write-Host "adr222 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr221.ps1"), (Join-Path $root "push-adr221.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-221-nothing-a-client-sends-closes-a-door-2026-09-17.md
  if ($st) { Write-Host "adr221 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-222-a-command-says-when-and-from-what-2026-09-17.md `
  docs/CHANGELOG-2026-09-17-adr222-a-command-says-when-and-from-what.md `
  tools/harness_contract.py `
  tools/harness_mcp.py `
  tools/mutate_contract.py `
  tools/verify/verify_contract.py `
  tools/verify/verify_walk.py `
  tools/delivery/adr222.json `
  docs/AI_HARNESS.md `
  tools/harness_board.html `
  tools/mutant_ledger.json `
  tools/published.json `
  tools/verify/counts.json `
  tools/delivery_ledger.json `
  tools/push/push-adr222.ps1
git -C $csrbt commit -m "ADR-222: a command could not say when it stopped being wanted, or what it had been decided from" `
  -m "ADR-221 made the door refuse a field it does not know, and the two it refused first were the two a careful client reaches for. FlowersForever's gateway requires a deadline on every physical command and lets a desktop action name the observation it was decided from; both get a meaning here, optional, protocol 1.8. WHY A NAME IS NOT ENOUGH: ADR-188 stamps a positional selector so an index cannot silently mean a different control, and a NAME has no such guard and cannot -- it still resolves after a dialog has opened over the page or the row under it was swapped; what it resolves to is no longer what the caller looked at. if_stamp is the caller handing back the stamp every snapshot has carried since ADR-191: act only if the target is still this. The door takes one look, compares, and refuses stale, naming both stamps and since as the way to see what moved. THAT LOOK IS NOT SERVED AND NOT REMEMBERED -- kept as the session's baseline it would make the caller's next since diff against a snapshot the caller never saw and answer that nothing changed. The rung is checked first, so a caller that may not act is not given a look for asking; a target that cannot be looked at is unavailable and is not run blind; a caller that does not ask pays nothing. expires_at is an ISO-8601 instant WITH AN OFFSET -- not a number, not a quoted number, not a time with no zone -- and the deadline is the first moment a command is unwanted, not the last it is wanted. A RECEIPT IS SERVED WHATEVER THE CLOCK SAYS, because the act it records happened. Neither field is part of the command's identity, so a retry with a new deadline is the same request. Both refusals are stale, ADR-189's code, so the walk and the MCP map already carry it. Over MCP and HTTP both ride in the call's _meta and only those two keys are taken from it. The clock is injected, so the suite holds expired, AT the deadline and one second before without sleeping. verify_contract 196 to 221, mutate_contract 87 to 103, verify_walk holds protocol 1.8." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015Ryyc7gWVh4QXAQ9RAFPF1"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR222: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr222.json)) { Write-Error "ADR222: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR222: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr221.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR222 pushed."
