# push-adr149.ps1 -- generated by tools/deliver.py from tools/delivery/adr149.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr149.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr148.ps1"), (Join-Path $root "push-adr148.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- tools/tasks/page-ecology-lab-science.json
  if ($st) { Write-Host "adr148 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  tools/tasks/page-releve-science.json `
  tools/entry_ledger.json `
  tools/readable_ledger.json `
  tools/task_ledger.json `
  tools/mutant_ledger.json `
  tools/delivery_ledger.json `
  tools/delivery/adr149.json `
  tools/verify/counts.json `
  tools/harness_board.html `
  tools/published.json `
  docs/ADR-149-the-releve-recorded-whole-2026-09-06.md `
  docs/CHANGELOG-2026-09-06-adr149-the-releve-recorded-whole.md `
  docs/AI_HARNESS.md `
  tools/push/push-adr149.ps1
git -C $csrbt commit -m "ADR-149: the releve, recorded whole -- seven published methods on one sheet" `
  -m "The fourth page taken whole, and the densest in published method of any in the kit. releve.html was the largest data-entry gap left after ADR-148 at 7 of 42 fields, and it carries more of other people's arithmetic than any other page: Braun-Blanquet and Daubenmire cover classes, Shannon and Pielou over summed midpoint cover, a growth-form spectrum, Swink & Wilhelm's Floristic Quality Index, Miller & Wardrop's adjusted FQI, the Corps of Engineers prevalence index with its 3.0 threshold, line-point intercept cover on a planned denominator, and Chapman & Wieczorek's point-radius. Seven named methods, one sheet, and the task drove three cover classes. It goes 30 steps to 109: 7 -> 44 OF 44 FIELDS, 45 -> 159 confirmed expectations, and the kit 312 -> 349 of 520, 60% -> 67%. Four pages are now entered whole: collection sheet (59 of 63, four reagent rows deliberately blank), stand sheet, ecology lab and releve. HELD, every figure recomputed by a Python port of the page's own arithmetic: the header line by line, including '# site: aspect 225 deg, slope 18%, xeric, gravelly loam over limestone colluvium, bare 25%, litter 35%'; three taxa read 3 taxa in 2 families, Shannon H' 0.53, evenness J' 0.48, 1.7 effective taxa, one non-native and the dominant at 80% of recorded cover, with 80% of that cover shrub; a fourth taxon at class 1 moves them to 0.67, 0.48, 1.9 and 77%, and an add with no cover class adds no row; Floristic Quality Assessment over two of three natives is mean C 4.50, FQI 6.4, adjusted FQI 31.8 and says so is partial, and over three of three it is 4.33, 7.5 and 37.5; scored UPL/FACU the prevalence index is 4.77, above the threshold; a 50 m transect read at 1 m in duplicate plans 100 points and at 0.5 m plans 200, and five top-canopy hits with four bare readings on the 100-point denominator are 5% foliar cover and 4% bare with 91 points still to read; a 40.5853/-111.6383 fix with 30 m of GPS on a 100 m2 plot is 43 m of coordinate uncertainty, of which 7 m is the plot itself, and 5402 m with the datum unknown; and a pack missing sci, gf and nat is rejected whole with the flora untouched. THREE CLAIMS THE SHEET MAKES THAT NOTHING WAS CHECKING. (1) THE SCALE IS A METHOD PARAMETER: Braun-Blanquet's class 2 and Daubenmire's class 2 are BOTH 5-25% and BOTH a midpoint of 15%, which the page's own method notes state, so the recorded class alone does not say what was measured. The sheet writes the scale into the export, and the task now switches to Daubenmire and back and holds both method lines. (2) BLANK MEANS UNSCORED, AND EMPTY MEANS NOTHING RECORDED: with no taxa the FQA and wetland boxes hold NOTHING AT ALL -- not a zero index, not a default -- and with two of three natives scored the FQA reports 'Only 2 of 3 natives have a C-value, so this is a partial score' and names the count. The page's own words are that a blank is honest and a guess is not. (3) COVER COMES FROM THE PLANNED DENOMINATOR, NOT FROM WHAT WAS READ: five top-canopy hits on a 100-point plan is 5% cover, not 100% of the five points read, and the sheet says 91 points still to read, so the percentages are marked partial until the transect is finished. Reporting cover from an unfinished transect is the standard way to get a confidently wrong number out of this method, and the page refuses to. THE ORACLE GREW A SECOND FORMATTER. ADR-148 established that the lab prints through Number.toLocaleString, which rounds half away from zero on the shortest round-trip decimal of the double. The releve prints through toFixed, which KEEPS TRAILING ZEROS: mean C is 4.33, and an all-FAC plot's prevalence index is 3.00 and not 3. Two formatters, in one kit, in adjacent pages; the oracle now ports both and each figure is compared against the one its own page uses. Held: a held index is not a VALIDATED one -- the FQI is computed from C-values a regional expert panel assigns and the page will not invent them, so holding the arithmetic says nothing about whether the C-values are right for the region, which is a question about the list rather than the sheet; the prevalence index is ONE OF THREE CRITERIA and the sheet says so in the same box as the number, which the task holds as well, because a number that travels without that sentence is the failure mode of the method; and experiment-guide at 16 of 52 is now the largest gap left, followed by deployment-log at 17 of 37." `
  -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_01CNn3hvazSDBU2TCgsGXjTt"
git -C $csrbt push
$t = Join-Path $root "_to_delete\adr128.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr129.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr130.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr131.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr132.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr133.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr134.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr135.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr136.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr137.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr138.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr139.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr140.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr141.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr142.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr143.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr144.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr145.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr146.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr147.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr148.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
$t = Join-Path $root "_to_delete\adr149.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR149 pushed."
