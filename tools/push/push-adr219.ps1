# push-adr219.ps1 -- generated by tools/deliver.py from tools/delivery/adr219.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr219.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr219.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr219 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR219 pushed."; exit 0 }
  Write-Host "adr219 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr217.ps1"), (Join-Path $root "push-adr217.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-217-a-script-that-said-it-pushed-2026-09-16.md
  if ($st) { Write-Host "adr217 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-219-a-claim-that-never-expired-2026-09-17.md `
  docs/CHANGELOG-2026-09-17-adr219-a-claim-that-never-expired.md `
  tools/audit_delivery.py `
  tools/deliver.py `
  tools/keep_emit.py `
  tools/mutate_delivery.py `
  tools/verify/verify_delivery.py `
  tools/delivery/adr219.json `
  docs/AI_HARNESS.md `
  tools/harness_board.html `
  tools/mutant_ledger.json `
  tools/published.json `
  tools/verify/counts.json `
  tools/delivery_ledger.json `
  tools/badinput_ledger.json `
  tools/carried_ledger.json `
  tools/destructive_ledger.json `
  tools/entry_ledger.json `
  tools/outputs_ledger.json `
  tools/readable_ledger.json `
  tools/restored_ledger.json `
  tools/takeaway_ledger.json `
  tools/push/push-adr219.ps1
git -C $csrbt commit -m "ADR-219: origin/main was red for ten slices, and the audit built to see that called it 'in flight'" `
  -m "A fresh clone of origin/main at ADR-217 fails verify_keep 270 of 271 while the ledger in the same commit says 316 of 316, and both are true: the 316 was counted on a disk carrying a change to tools/keep_emit.py that ADR-207 made and no push script ever staged. audit_delivery exists to find exactly that file (ADR-147), ran in every full run since, and said 0 UNDELIVERED every time. Its docstring says it subtracts the paths the manifests of UNSHIPPED slices claim, and its code read EVERY manifest, forever: ADR-206's manifest names keep_emit.py, ADR-206 shipped, ADR-207 changed the file without naming it, and the audit filed the change under in flight. By ADR-217 that bucket held 221 files -- the contract, every ledger, the board, AI_HARNESS -- so the audit could still see a brand-new file nobody had named and nothing else. The ratchet under it had stopped too: claims were meant to end when --record moved the ledger, and the last slice recorded is ADR-190; --record was a step in every close and a check in none, ADR-216's shape exactly. NOW A CLAIM EXPIRES WHEN ITS SLICE SHIPS: only an unshipped manifest claims its paths. AND WHERE THERE IS GIT, GIT IS ASKED -- which bytes are not HEAD's, which manifests are in HEAD's tree; no git, no repository, no commit or a git that failed is NO EVIDENCE, never clean, and the audit says which evidence it read. Without git a slice is over when --record says so BY NAME, not inferred from which slice a path was last delivered by -- the first version did that and the suite's own older fixture refuted it five times, because an adoption writes by too. --catch-up brings a stalled ledger up to what git vouches for and nothing else (217 paths, 71 slices), will not record a modified file, and refuses with no git. --check holds the recording step and named 27 slices. First honest run: 654 delivered, 222 in flight, 0 undelivered became 870, 0 and 1. AND ONE MORE IT CANNOT SEE: AI_HARNESS carries a paragraph headed ADR-218 whose slice -- tools/exempt.py, tools/audit_declared.py, verify_declared -- exists NOWHERE; four shared paths carry its traces, among them run_all, which runs an audit_declared that is not there, so a full run on origin has a red row for a second reason. ADR-217's commit staged those paths with the next slice's edits already in them. That is why this slice is 219. The audits rewrite their ledgers on every full run and a slice that ran them ships them, since git now says so. verify_delivery 50 to 66, mutate_delivery 38 to 53 all killed." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015Ryyc7gWVh4QXAQ9RAFPF1"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR219: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr219.json)) { Write-Error "ADR219: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR219: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr217.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR219 pushed."
