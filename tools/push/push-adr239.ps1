# push-adr239.ps1 -- generated by tools/deliver.py from tools/delivery/adr239.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr239.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr239.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr239 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR239 pushed."; exit 0 }
  Write-Host "adr239 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr238.ps1"), (Join-Path $root "push-adr238.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-238-the-next-bird-says-what-it-will-carry-2026-09-22.md
  if ($st) { Write-Host "adr238 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-239-a-name-outlives-the-press-2026-09-22.md `
  docs/CHANGELOG-2026-09-22-adr239-a-name-outlives-the-press.md `
  docs/AI_HARNESS.md `
  docs/field-notebook.html `
  docs/farm-scout.html `
  tools/harness_plugin_page.py `
  tools/audit_addresses.py `
  tools/address_ledger.json `
  tools/verify/verify_addresses.py `
  tools/mutate_addresses.py `
  tools/harness_board.py `
  tools/verify/run_all.py `
  tools/tasks/page-field-notebook-science.json `
  tools/tasks/page-farm-scout-science.json `
  tools/delivery/adr239.json `
  tools/harness_board.html `
  tools/mutant_ledger.json `
  tools/published.json `
  tools/verify/counts.json `
  tools/delivery_ledger.json `
  tools/badinput_ledger.json `
  tools/carried_ledger.json `
  tools/destructive_ledger.json `
  tools/entry_ledger.json `
  tools/outputs_ledger.json `
  tools/readable_ledger.json `
  tools/restored_ledger.json `
  tools/takeaway_ledger.json `
  tools/push/push-adr239.ps1
git -C $csrbt commit -m "ADR-239: a name outlives the press -- every addressed control is pressed and its name read back; the door re-stamps before it resolves, and the tally cards are named by what they count, not by their count" `
  -m "ADR-188's addresses were checked when published and never a moment later. The field notebook's tally card read species-a0 and one tap made it species-a1; its task had fallen back to specGrid#0. New audit_addresses presses every addressed control of every page through the door and reads the old name back: held, gone, renamed or shadowed. Its first reading found the door itself: data-h was stamped only at observe, so a press that rebuilt a list left a just-published name answering 'no control answers to it'; _resolve now re-stamps first. Then two pages whose tally cards were named by their count (field notebook 7, farm scout 4), and steppers named only '-' and '+'. Kit: 1,632 presses, 1,615 held, 17 gone, 0 renamed, 0 shadowed. Both tasks press by name; new verify_addresses (36) and mutate_addresses (12/12)." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015Ryyc7gWVh4QXAQ9RAFPF1"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR239: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr239.json)) { Write-Error "ADR239: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR239: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr238.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR239 pushed."
