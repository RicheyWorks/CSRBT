# push-adr241.ps1 -- generated by tools/deliver.py from tools/delivery/adr241.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr241.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr241.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr241 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR241 pushed."; exit 0 }
  Write-Host "adr241 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr240.ps1"), (Join-Path $root "push-adr240.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-240-a-budget-file-is-the-whole-budget-2026-09-22.md
  if ($st) { Write-Host "adr240 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-241-the-count-says-which-tree-it-counted-2026-09-22.md `
  docs/CHANGELOG-2026-09-22-adr241-the-count-says-which-tree-it-counted.md `
  docs/AI_HARNESS.md `
  tools/evidence.py `
  tools/verify/floors.json `
  tools/verify/run_all.py `
  tools/harness_board.py `
  tools/verify/verify_board.py `
  tools/verify/verify_evidence.py `
  tools/mutate_evidence.py `
  tools/mutate_board.py `
  tools/delivery/adr241.json `
  tools/harness_board.html `
  tools/mutant_ledger.json `
  tools/published.json `
  tools/verify/counts.json `
  tools/delivery_ledger.json `
  tools/badinput_ledger.json `
  tools/carried_ledger.json `
  tools/destructive_ledger.json `
  tools/entry_ledger.json `
  tools/outputs_ledger.json `
  tools/readable_ledger.json `
  tools/restored_ledger.json `
  tools/takeaway_ledger.json `
  tools/address_ledger.json `
  tools/push/push-adr241.ps1
git -C $csrbt commit -m "ADR-241: the count says which tree it counted -- every suite count is bound to a digest of the tree it measured, every suite has a floor it may not shrink below, and the board's verdict gates on both (ported from the FlowersForever harness)" `
  -m "A review of the FlowersForever harness found two things it does that this kit did not: its evidence is bound to a digest of every build input, refused on --check from any other tree and invalidated by an edit during verification; and every test class has a committed floor so a growing class cannot hide a deleted one. This kit's counts bound each suite count to the suite's own source and never to what it measured, so an edited page or tool left the board green about a tree that no longer existed, and the ADR-238 close hand-edited a counts entry. tools/evidence.py digests the subject and not the evidence; run_all takes the tree before the jobs, writes it with every count, hands it to every job, applies the floors, and fails a run the tree moved under; run_all --only records one job the way the run does; the board gains two gates and names the changed files. New verify_evidence 28, verify_board 225 to 234, new mutate_evidence 13/13." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015Ryyc7gWVh4QXAQ9RAFPF1"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR241: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr241.json)) { Write-Error "ADR241: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR241: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr240.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR241 pushed."
