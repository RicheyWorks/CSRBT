# push-adr216.ps1 -- generated by tools/deliver.py from tools/delivery/adr216.json.
# DO NOT EDIT: `python3 tools/deliver.py --check` compares this file with what
# the manifest generates, and a hand edit is a failure. Change the manifest.
#
# Run from anywhere:   .\CSRBT\tools\push\push-adr216.ps1
$ErrorActionPreference = "Stop"
$csrbt = (Resolve-Path (Join-Path $PSScriptRoot "..\..")).Path
$root  = (Resolve-Path (Join-Path $csrbt "..")).Path
$lock = Join-Path $csrbt ".git\index.lock"; if (Test-Path $lock) { Remove-Item $lock -Force }
# THIS SCRIPT PUSHES ONCE (ADR-184). Run a second time, an earlier version of a
# script like this committed the NEXT slice's changes to the paths the two share
# under this slice's message and left that slice's own files behind. The slice is
# committed once its manifest is in HEAD; after that, only an undelivered push is
# this script's to finish.
$mine = git -C $csrbt ls-tree HEAD -- tools/delivery/adr216.json
if ($mine) {
  $ahead = git -C $csrbt rev-list --count "@{u}..HEAD"
  if ([int]$ahead -gt 0) { Write-Host "adr216 is committed but not pushed -- pushing"; git -C $csrbt push; Write-Host "ADR216 pushed."; exit 0 }
  Write-Host "adr216 is already pushed -- nothing to do (modified paths belong to a later slice; run its script)"; exit 0
}
# The slice before this one, wherever its script lives: in the repo from
# ADR-147 on, in the parent directory for ADR-112 to ADR-146. It is run only
# if the file it was supposed to commit is still uncommitted.
$prev = @((Join-Path $PSScriptRoot "push-adr215.ps1"), (Join-Path $root "push-adr215.ps1")) | Where-Object { Test-Path $_ } | Select-Object -First 1
if ($prev) {
  $st = git -C $csrbt status --porcelain -- docs/ADR-215-which-numbers-decide-the-verdict-2026-09-16.md
  if ($st) { Write-Host "adr215 is not committed yet -- running its script first"; & $prev }
}
git -C $csrbt add -A `
  docs/ADR-216-a-ratchet-that-did-not-exist-2026-09-16.md `
  docs/AI_HARNESS.md `
  docs/CHANGELOG-2026-09-16-adr216-a-ratchet-that-did-not-exist.md `
  tools/contend.py `
  tools/contention_ledger.json `
  tools/delivery/adr216.json `
  tools/harness_board.html `
  tools/harness_board.py `
  tools/published.json `
  tools/push/push-adr216.ps1 `
  tools/verify/counts.json `
  tools/verify/run_all.py `
  tools/verify/verify_board.py `
  tools/verify/verify_contend.py
git -C $csrbt commit -m "ADR-216: the fix for a reading that looked like a failure was a sentence asserting a ratchet that did not exist" `
  -m "ADR-215, an hour old, made every tile on the Harness Board say whether it gates the verdict, and the one that started it -- 6 / 7 clean under load -- got the line 'a failed run out of fifty-two is a known flake with a ratchet, not a red suite'. THERE WAS NO RATCHET: contention_ledger.json had no ceiling, no declaration and nothing that ran it, and contend.py was the only instrument in this kit whose readings nothing checked on a schedule. ADR-203's shape and ADR-212's exactly -- a message asserting what cannot be observed -- except that those were found in code written months earlier and this one was written WHILE FIXING THE PREVIOUS SLICE. What was actually there was one honest reading: verify_organism beside verify_tie_render, 16 runs, 1 failed, with the check named and the co-tenant named. What was missing is what happens NEXT: that reading had been true for weeks and would have gone on being true at 2, at 5, at 20, with the board printing the ratio and no rule deciding when it stopped being acceptable. A flake that is getting worse is a race that is getting likelier, and the difference between a flake and a claim that is false when the machine is busy is how often it happens (ADR-142's own words). Every pairing now carries a ceiling that only comes down; a pairing with no ceiling yet is not above one. contend.py bare now reports so run_all can run it -- a ceiling nothing checks is a number, not a ratchet -- while the slow sweep stays opt-in. The one failure is NOT declared: a declaration would say it is right to fail beside another suite, and it is held at the count it has reached. verify_board requires that tile's line to NAME the rule that holds it; verify_contend 36 to 42." `
  -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_01YPcb1A7CejriRgL9xLrhJ3"
if ($LASTEXITCODE -ne 0) { Write-Error "ADR216: git commit failed ($LASTEXITCODE) -- nothing was committed and nothing will be pushed"; exit 1 }
if (-not (git -C $csrbt ls-tree HEAD -- tools/delivery/adr216.json)) { Write-Error "ADR216: the commit ran and this slice's manifest is not in HEAD -- refusing to report a push that did not happen"; exit 1 }
git -C $csrbt push
if ($LASTEXITCODE -ne 0) { Write-Error "ADR216: git push failed ($LASTEXITCODE) -- the commit is local and the remote does not have it"; exit 1 }
$t = Join-Path $root "_to_delete\adr215.tgz"; if (Test-Path $t) { Remove-Item $t -Force }
Write-Host "ADR216 pushed."
