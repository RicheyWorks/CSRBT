# 2026-08-20 — frontend verification: the pages, driven; the artifacts, regenerated

The three HTML pages had not been driven in a browser since the seventh pass, and both
`ecology-lab.html` and `ExperimentLab` changed during the stale-clone correction — while the
checked-in sample bundle carried a tell: text artifacts stamped by the old base, but
`report.pptx`/`workbook.xlsx` stamped 2026-08-19 08:38 — generated by the STALE batch during
the incident. This pass regenerated everything on the corrected base and drove every page in
real headless Chromium (Playwright), with console errors as failures.

## Finding (fixed): the ecologyExperiment task lost its canonical outputs

The correction merge restored the J4 `ExperimentLab` (safe sibling-of-the-spec default
paths) but kept the stale batch's `build.gradle.kts`, whose task passed only the spec —
built against the old fixed-path ExperimentLab, it never needed more. Result: regenerating
the shipped sample would have silently written `sample-experiment-session.json` beside the
spec instead of the canonical checked-in artifacts. The task now implements J4's actual
design: no `-Pspec` → the canonical output paths are passed EXPLICITLY (naming the
destination is how you say "overwrite the shipped bundle"); a student's `-Pspec` gets the
safe default.

## The artifacts, regenerated on the corrected base

`./gradlew ecologyExperiment` — every text artifact came out **byte-identical in size** to
the pre-incident bundle (session.json 9,072B, report.html 44,569B, all nine CSVs matching):
the byte-reproducibility discipline, holding across the correction. The office files
(structure-pinned, not byte-pinned) verified structurally: 9 typed sheets in
`workbook.xlsx` (phases, data, drift, notes, trees, model-series, punnett, crosses,
hypotheses), 4 slides in `report.pptx` (title, phases, graded hypotheses, field notebook).

## The pages, driven (headless Chromium, console errors = failure)

| page | drive | verdict |
|---|---|---|
| `docs/ecology-lab.html` | load + DROP the regenerated session onto it | **clean** — the lab rendered the graded-hypotheses section byte-for-byte matching the generator's console verdicts (✅/❌ and observed values identical) |
| `demo/visualizer.html` | load + feed `arena-session.json` via its file input | **clean** |
| `docs/tree-visualizer.html` | load; canvas present and rendering | **clean** |
| `docs/experiment-out/report.html` | load the regenerated report | **clean** — 27k chars of rendered content |

Zero console errors, zero page errors, on all four.

## Verdict

The corrected frontend surface is verified end to end: generator → canonical artifacts →
lab page, with the drop path exercised for real and the wiring gap the correction had left
in the Gradle task closed.
